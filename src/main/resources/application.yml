spring:
  datasource:
    url: jdbc:mysql://kgproject.cxjpnucg86bc.us-west-1.rds.amazonaws.com:3306/KGdb
    username: admin
    password: 11111111

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
    properties:
      hibernate.format_sql: true

  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  jackson:
    serialization:
      fail-on-empty-beans: false

server:
  port: 5000
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
