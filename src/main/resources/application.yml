spring:
  datasource:
    url: jdbc:mysql://kgproject.cp9xol0loow4.us-east-2.rds.amazonaws.com:3306/KGProjectdb
    username: admin
    password: 11111111

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
    properties:
      hibernate.format_sql: true

  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  jackson:
    serialization:
      fail-on-empty-beans: false

server:
  port: 5000
  servlet:
    context-path: /
    encoding:
      charset: UTF-8